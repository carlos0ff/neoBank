package br.com.neo.utils;

import java.util.regex.Pattern;


/**
 * Classe responsável por validar entradas do usuário e garantir que os dados fornecidos atendam a determinados critérios.
 * A classe contém métodos estáticos que podem ser utilizados para validar CPF, e-mail e outros tipos de dados comuns.
 */
public class Validator {


    /**
     * Valida um número de CPF.
     * O método verifica se o CPF possui o formato correto (apenas números) e se é válido com base em um cálculo de verificação.
     * 
     * @param cpf O CPF a ser validado
     * @return true se o CPF for válido, false caso contrário
     */
    public static boolean isValidCPF(String cpf){ }
}